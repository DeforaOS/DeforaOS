subdirs=DeforaOS Manual Pages
targets=build,configure,download,extract,patch
dist=Makefile,script.sh

#targets
[build]
type=command
#XXX work around the spaces in the directory name
command=cd $(SUBDIR) && $(MAKE) build
phony=1

[configure]
type=command
command=cd $(SUBDIR) && $(MAKE) configure
phony=1

[download]
type=command
command=cd $(SUBDIR) && $(MAKE) download
phony=1

[extract]
type=command
command=cd $(SUBDIR) && $(MAKE) extract
phony=1

[patch]
type=command
command=cd $(SUBDIR) && $(MAKE) patch
phony=1
